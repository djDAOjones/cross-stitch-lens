# M15-UI-03 — Editor content: libraries, pins, ranges, readout

Scope parent: D114. Lands inside UI-02's shell. Draft editing per
the signed scope: Save commits to the library, the editor's own
preview applies live.

## Anatomy (signed at scope)

```text
Header (UI-02) · test-preview strip (UI-04)
LIBRARIES column                  | RESULTING COLOURS
  Thread brands (8, provenance)   |  count + per-step explanations
  Colour maps (6, entry counts)   |  swatch grid, capped + search
  My threads (inventory count)    |  (the 60-row pattern)
  Add colour: hex/code search     |  pins listed with remove
  [ ] Only colours I own          |
  H/S/B two-pole range sliders    |
```

- Browse per library row reuses the capped search table
  (palette-panel pattern); the row affordance is include/exclude
  pin toggles. No Own editing here — the inventory stays its own
  concern.
- Custom add: hex/code search over catalogue + name table; a miss
  offers "Add #RRGGBB as a custom colour" → a `user:` entry.
- Ranges: two-pole sliders (hue wraps) with numeric fields beside
  them (AAA — never drag-only); the explanation line updates live.
- The readout is the never-silent surface: every narrowing is a
  sentence, severity carried by words (UI-STANDARDS conflict
  pattern).

## Done when (expanded)

- Draft edits update readout and preview live; Save commits exactly
  one revision bump; Cancel restores the pre-open state exactly.
- All controls AAA: ≥ 44 px targets, visible labels matching
  accessible names, fully keyboard-operable; swatches carry
  hex + name on hover/focus.
- Custom colours render with `user:` identity and honest labels.
