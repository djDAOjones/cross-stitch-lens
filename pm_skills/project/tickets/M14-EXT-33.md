# M14-EXT-33 — Capture section recut: rename, always-open start, session controls return

Memo items 1 + 4 + 5 (fifth look, D109). One task: all three land on
the same section in the same files.

## Outcome

1. **"Capture region" renames to "Capture"** (memo 5). Section id
   `section-capture` stays (no preference migration); the crop
   overlay's own accessible name ("Capture region") can stay — it
   names the rectangle, not the section.
2. **The section starts every session expanded** (memo 1),
   superseding D97's persisted-collapse-on-remount: `showCaptureSection`
   forces open at mount. Recommendation: keep the mid-session toggle
   working but stop persisting this section's disclosure — a stored
   collapse that only ever gets overridden is a dead preference.
   (Watch the interaction with M14-FIX-01's collapse-hands-back-the-
   lead gesture — that stays.)
3. **The session controls return to the section** (memo 4 — "move
   'capturing source' to the capture region section"): Stop capture ·
   Pause/Resume capture · Capture frame render in the section's
   control row beside Lock aspect · Lock region. The Source button
   reads **"Source" at all times** and its modal drops the session
   block — D108's option A superseded on the owner's own authority at
   the fifth look, having seen it live. The D108 fixed point ("Stop
   reachable from the bar at all times") is consciously given up by
   this move; record it, and name it for ACCEPT-01.

## Reading recorded for the gate

"Capturing source" is read as the shipped **"Capturing — Source"**
bar-button state (the only thing by that name in the live UI) and
"move" as relocating the whole session affordance it fronts. The
narrower reading — only a "Capturing {label}" status line moves, the
modal keeps the session block — contradicts "move" (it would
duplicate) and the `captureMeta` line already sits in the section;
noted so the gate conversation starts from the owner's words.

## Constraints

- One owner per control (D90): the controls move, never duplicate —
  the modal loses what the section gains.
- Named states hold: "paused", "live update stopped" copy unchanged;
  the pump-death recovery copy re-points from "the Source menu" back
  to the section's own button.
- Every control: visible text label, 44 px target, announced state
  (`aria-pressed` on Pause returns with the button).
- The D105/D108 focus-rescue paths in `endCaptureUi` re-verify: Stop
  now fires from inside the section again, so the
  `focusWasInCapture` branch is live once more.

## Files

`src/main.ts` (captureRow, Source modal choices, stopSession/
togglePauseCapture wiring, showCaptureSection, section title),
`docs/ui-spec.md` §2/§5 rows.
