# M15-UI-01 — Colour section recut: profile select and the (edited) state

Scope parent: D114; run order D115 (the cutover lands last, before
acceptance, on the section M14's EXT-41/42 leave behind — M14
completes first). Line compressed into this ticket at the 2026-08-07
roadmap refactor (D119); full intent preserved here.

## The section after the cutover (control census)

- **Profile select** + **Edit profiles…** into the finished editor.
- **"(edited)" state:** editing the design's copy badges the select;
  actions Update profile / Save as new / Revert.
- **Count** and the new **minimum distance** rule (M15-CORE-03).
- **Must-use chips** with a search-to-add field — a guaranteed colour
  need not be in use yet.
- **Colours used** gains a Remove-from-profile row action, landing on
  the design's copy.

## Rules

- The old controls swap out **only here** — atomic cutover, no dead
  controls at any point of the milestone; fills the slot EXT-42
  protects.
- No shared-library mutation without an explicit Update / Save as
  new.
- Non-thread entries render honestly in Colours used (CORE-01
  labels).

## Done when (expanded)

- Every path announces; chips keyboard-operable including
  add-by-search.
- The section is materially no taller than EXT-42's result.
