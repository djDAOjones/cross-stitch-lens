# Doc-deltas — pm-skills framework repository

<!-- Capture-only ledger of pending protected-doc reconciliations.
     Cold tier — only the open-count line is surfaced at session
     start; full read happens in a doc-sync pass
     (pm_skills/prompts/memory-maintenance.md). One checkbox line per
     delta, oldest at the top; never write edit instructions here. -->

## Open

<!-- Append captured deltas below, one checkbox line each. Delete this
     comment once you add the first real item. -->
